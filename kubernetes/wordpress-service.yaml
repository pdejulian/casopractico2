# =============================================================================
# wordpress-service.yaml - Expone la app a Internet por HTTPS (puerto 443)
# =============================================================================
apiVersion: v1
kind: Service
metadata:
  namespace: default
  name: wordpress-svc
spec:
  type: LoadBalancer
  selector:
    app: wordpress
  ports:
    - port: 443
      targetPort: 443
